#### The raw data and intermediate files used for constructing the LPPI network and benchmarking datasets are stored here
- `benchmark`: stores the constructed human and mouse benchmark datasets
- `LPI`: stores the processed LPI data
- `PPI`: stores the processed PPI data
- `LPPI`: stores the processed LPPI data
- `raw`: stores the raw data